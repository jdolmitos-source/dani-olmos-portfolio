rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read published projects (needed for the public portfolio).
    // Only your logged-in admin account can create, edit, or delete them.
    match /projects/{projectId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
